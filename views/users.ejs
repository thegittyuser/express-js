<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <p><%= description %></p>

    <%let items = ["Red", "Apple", "Green", "Football", "Brown", "Red",
    "PineAplle"];%>

    <ul>
      <% items.forEach((item) => { %>
      <li><%= item %></li>
      <% }) %>
    </ul>
  </body>
</html>
